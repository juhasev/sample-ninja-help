### SETTINGS

Choose to toggle whether the new Data variable is Personally Identifiable (PI) and/or whether this variable is available within the Community API (writable)

Within OPTIONS you choose the variable data choices by pressing (+ Icon). For example,  if generating a new **Salary** data variable for your Panel, click on (+ icon) and define the data bands for **Salary**. For example up to **$25,000 $25,001 -$50,000, $50,001 to $75,000, etc.**

Data variables can be added at any point in the lifetime of the Panel,  when initially setting up the panel or Data variables can be added at a later time as and when required. There are no limits to the number of data available is that one can have within the system.