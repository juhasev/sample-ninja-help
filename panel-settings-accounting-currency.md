### Accounting currency
This is the default currency used within the system for accounting purposes, reports and panelist redemptions.

This is where the Panel defines a Currency unit in relation to reward points within the system.  **for example: 1 point = $1  or alternatively, 100 points = $1**